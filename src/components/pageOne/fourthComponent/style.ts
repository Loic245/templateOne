import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    container: {
        background: "#333333",
        width: "100%",
        height: "30vh",
    },
    text: {
        color: "#fff",
        fontWeight: 400,
        textAlign: "center",
        padding: '2.8rem 0'
    }
});

export default useStyles;
