import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    container: {
        background: "#1a1a1a",
        width: "100%",
        height: "80vh",
        padding: "4rem 0",
        overflow: "hidden",
    },
    subtitle1: {
        color: "#e73d0c",
        fontWeight: 900,
        padding: "1rem 0"
    },
    subtitle2: {
        color: "#fff",
        fontWeight: 400,
    },
    item: {
        padding: "0 8rem",
    }
});

export default useStyles;
